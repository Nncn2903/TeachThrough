
  # TeachThrough

  This is a code bundle for TeachThrough. The original project is available at https://www.figma.com/design/QHjwvCC5L7mn2f2x7wedKm/TeachThrough.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  